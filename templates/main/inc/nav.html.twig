<nav class="header-nav">
    <ul>
        <li><a href="{{ path('main_home') }}" title="Home">Home</a></li>
        <li><a href="{{ path('wish_list') }}" title="Wish list">Wish list</a></li>
        {% if app.user %}
            <li><a href="{{ path('wish_add') }}" title="Add a wish">Add a wish</a></li>
            <li><a href="{{ path('app_logout') }}" title="Logout">Logout {{ app.user.getUserIdentifier }}</a></li>
        {% else %}
            <a href="{{ path('app_register') }}" title="Create your account">Register</a>
            <a href="{{ path('app_login') }}" title="Login">Login</a>
        {% endif %}
        <li><a href="{{ path('main_aboutUs') }}" title="About Us">About us</a></li>
    </ul>
</nav>